import React from 'react';

const Header = () => {
    return (
        <header style={{ marginBottom: '2rem' }}>
            <h1>Descargador MP3/Video</h1>
        </header>
    );
};

export default Header;
